name: Build & Push UI Dockers

on:
  workflow_dispatch: 
    inputs:
      service_file:
        description: 'Path to the service spec file (e.g. staff-portal-ui/registry-staff-portal-ui-v1.0.0.txt)'
        required: false
        type: string
        default: 'staff-portal-ui/registry-staff-portal-ui-v1.0.0.txt'
      dockerfile:
        description: 'Path to the Dockerfile (optional, defaults to Dockerfile in same dir)'
        required: false
        type: string

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - id: set-matrix
        shell: python
        env:
          INPUT_SERVICE_FILE: ${{ inputs.service_file }}
          INPUT_DOCKERFILE: ${{ inputs.dockerfile }}
        run: |
          import os
          import json
          import sys

          service_file = os.environ.get('INPUT_SERVICE_FILE', '').strip()
          dockerfile = os.environ.get('INPUT_DOCKERFILE', '').strip()

          matrix_include = []

          if service_file:
              # Manual run with specified file
              name = os.path.basename(service_file).replace('.txt', '')
              matrix_include.append({
                  "name": name,
                  "serviceFile": service_file,
                  "dockerfile": dockerfile
              })
          else:
              # Default fallback
              matrix_include.append({
                  "name": "staff-portal-ui",
                  "serviceFile": "staff-portal-ui/registry-staff-portal-ui-v1.0.0.txt",
                  "dockerfile": "staff-portal-ui/Dockerfile"
              })
             
          matrix = {"include": matrix_include}

          with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
            f.write(f"matrix={json.dumps(matrix)}\n")
            
  docker-build:
    needs: setup
    runs-on: ubuntu-latest
    name: Docker - ${{ matrix.name }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.setup.outputs.matrix)}}

    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.docker_hub_actor }}
          password: ${{ secrets.docker_hub_token }}

      - name: Diagnostics (token envs, docker, proxy, buildx)
        shell: bash
        run: |
          echo "== GH Actions runtime token envs (masked) =="
          env | sort | grep -E '^(ACTIONS_|GITHUB_|RUNNER_)' || true
          echo
          echo "== Proxy configuration =="
          env | sort | grep -E '^(HTTP|http|HTTPS|https|NO|no)_PROXY=' || true
          echo
          echo "== Docker info =="
          docker info || true
          echo
          echo "== Buildx version =="
          docker buildx version || true
          echo
          echo "== Builder info =="
          docker buildx ls || true

      - name: Read service file and prepare adapters requirements - ${{ matrix.name }}
        id: read
        shell: python
        env:
          SERVICE_FILE: ${{ matrix.serviceFile }}
        run: |
          import os
          import sys
          import re
          import subprocess
          from datetime import datetime

          service_file = os.environ['SERVICE_FILE']
          # For UI, Dockerfile is usually hardcoded or looked up, but here we can stick to defaults
          # or infer it. The bash script had logic to read lines. Let's replicate.

          if not os.path.exists(service_file):
              print(f"Service file not found: {service_file}", file=sys.stderr)
              sys.exit(1)

          with open(service_file, 'r') as f:
              lines = [line.strip() for line in f if line.strip()]

          if not lines or not lines[0].startswith('#!'):
              print("Invalid file format: First line must start with #!IMAGE_ID", file=sys.stderr)
              sys.exit(1)

          # Line 1: Image ID
          image_id = lines[0].lstrip('#!').strip()

          # Dockerfile resolution (similar to bash script logic)
          dockerfile = ""
          if len(lines) > 1 and lines[1].startswith('#!'):
             dockerfile = lines[1].lstrip('#!').strip()

          if not dockerfile:
             # Fallback
              service_dir = os.path.dirname(service_file)
              candidate = os.path.join(service_dir, 'Dockerfile')
              if os.path.exists(candidate):
                  dockerfile = candidate
              else:
                  # Try Dockerfile.ui as per old structure if Dockerfile not found
                  candidate_ui = os.path.join(service_dir, 'Dockerfile.ui')
                  if os.path.exists(candidate_ui):
                      dockerfile = candidate_ui

          if not dockerfile:
              print("Dockerfile path not found.", file=sys.stderr)
              sys.exit(1)

          repo_url = ""
          git_branch = ""

          for line in lines:
              if line.startswith("git://"):
                  m = re.match(r'git://([^/]+)//(.+)', line)
                  if m:
                      git_branch = m.group(1)
                      repo_url = m.group(2)
                      break

          if not repo_url or not git_branch:
              print("Failed to extract repo URL or branch", file=sys.stderr)
              sys.exit(1)

          print(f"Repo: {repo_url}")
          print(f"Branch: {git_branch}")

          # Git info
          try:
              commit_hash = subprocess.check_output(
                  ['git', '--no-pager', 'log', '-1', '--pretty=format:%H']
              ).decode('utf-8').strip()
          except:
              commit_hash = "unknown"

          created = datetime.utcnow().strftime("%Y-%m-%dT%H:%M:%SZ")

          vendor = image_id.split('/')[0] if '/' in image_id else "unknown"
          title = image_id.split('/')[1].split(':')[0] if '/' in image_id else image_id

          version = "latest"
          if ':' in image_id:
              version = image_id.split(':')[-1]

          # Write to GITHUB_OUTPUT
          github_output = os.environ.get('GITHUB_OUTPUT')
          if github_output:
              with open(github_output, 'a') as f:
                  f.write(f"image={image_id}\n")
                  f.write(f"dockerfile={dockerfile}\n")
                  f.write(f"context=.\n")
                  f.write(f"created={created}\n")
                  f.write(f"commit={commit_hash}\n")
                  f.write(f"vendor={vendor}\n")
                  f.write(f"title={title}\n")
                  f.write(f"version={version}\n")
                  f.write(f"repo_url={repo_url}\n")
                  f.write(f"git_branch={git_branch}\n")

      - name: Debug extracted values
        run: |
          echo "Repo URL: ${{ steps.read.outputs.repo_url }}"
          echo "Branch: ${{ steps.read.outputs.git_branch }}"

      - name: Build and push - ${{ matrix.name }}
        uses: docker/build-push-action@v6
        with:
          context: ${{ steps.read.outputs.context }}
          file: ${{ steps.read.outputs.dockerfile }}
          push: true
          platforms: linux/amd64
          tags: ${{ steps.read.outputs.image }}

          build-args: |
            REPO_URL=${{ steps.read.outputs.repo_url }}
            GIT_BRANCH=${{ steps.read.outputs.git_branch }}

          labels: |
            org.opencontainers.image.created=${{ steps.read.outputs.created }}
            org.opencontainers.image.authors=${{ github.repository_owner }}
            org.opencontainers.image.source=${{ github.repository }}@${{ steps.read.outputs.commit }}
            org.opencontainers.image.version=${{ steps.read.outputs.version }}
            org.opencontainers.image.revision=${{ steps.read.outputs.commit }}
            org.opencontainers.image.vendor=${{ steps.read.outputs.vendor }}
            org.opencontainers.image.title=${{ steps.read.outputs.title }}
            org.opencontainers.image.description=OpenG2P Registry Gen2 service image
            org.openg2p.service.name=${{ matrix.name }}
